services:
  focusBot:
    restart: always
    ports:
      - 8080:80
    environment:
      - CONFIG_PATH=/app/stuff
      - LOG_LEVEL=debug
    image: "ghcr.io/npmaile/focusbot/focusbot:latest"
    volumes:
      - ./stuff:/app/stuff
